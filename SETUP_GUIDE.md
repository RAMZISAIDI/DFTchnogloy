# DF Technology — دليل النشر الكامل

## 📁 هيكل الملفات
```
dftechnology/
├── index.html          ← الموقع الرئيسي
├── 404.html            ← صفحة الخطأ
├── robots.txt          ← SEO
├── sitemap.xml         ← SEO
├── netlify.toml        ← إعدادات النشر
├── supabase_schema.sql ← سكيما قاعدة البيانات
├── js/
│   └── db.js           ← طبقة قاعدة البيانات
├── admin/
│   ├── login.html      ← صفحة الدخول
│   └── index.html      ← لوحة التحكم
├── project/
│   └── index.html      ← صفحة مشروع منفردة
└── images/             ← الصور المحلية
```

---

## 🗄️ الخطوة 1: إعداد Supabase (مجاني)

1. اذهب إلى **https://supabase.com** → Sign up (مجاناً)
2. أنشئ مشروعاً جديداً:
   - **Name:** dftechnology
   - **Database Password:** اختر كلمة مرور قوية واحفظها
   - **Region:** اختر أقرب منطقة (Frankfurt - EU)
3. انتظر ~2 دقيقة حتى يكتمل الإنشاء

### إنشاء الجداول:
4. من القائمة الجانبية → **SQL Editor** → New Query
5. افتح ملف `supabase_schema.sql` وانسخ كل المحتوى
6. الصقه في محرر SQL واضغط **RUN** ✓

### إنشاء Storage Bucket للصور:
7. من القائمة → **Storage** → New Bucket
   - Name: **media**
   - ✅ Public bucket
8. اضغط **Save**

### الحصول على المفاتيح:
9. من القائمة → **Settings** → **API**
10. انسخ:
    - **Project URL** → `https://xxxx.supabase.co`
    - **anon public** key → `eyJhbGci...`

---

## 🌐 الخطوة 2: نشر الموقع (Netlify — مجاني)

1. اذهب إلى **https://netlify.com** → Sign up
2. اسحب **مجلد** `dftechnology` كاملاً وأفلته في صفحة Netlify
3. انتظر 30 ثانية → الموقع جاهز!

### تخصيص الدومين (اختياري):
4. من Netlify → Site Settings → Domain Management
5. اضغط **Add custom domain** وأدخل نطاقك

---

## ⚙️ الخطوة 3: ربط قاعدة البيانات من لوحة التحكم

1. اذهب إلى `yoursite.com/admin/login.html`
2. الدخول: **admin / DFtech2026**
3. من القائمة → **إعداد قاعدة البيانات**
4. أدخل:
   - **Supabase URL:** `https://xxxx.supabase.co`
   - **Anon Key:** `eyJhbGci...`
5. اضغط **اختبار الاتصال** للتحقق
6. اضغط **حفظ وتطبيق**
7. أعد تحميل الصفحة ✓

---

## 🔐 الخطوة 4: تأمين الموقع

1. من لوحة التحكم → **الأمان وكلمة المرور**
2. غير:
   - اسم المستخدم: `admin` → اسمك
   - كلمة المرور: `DFtech2026` → كلمة مرور قوية

---

## 📝 الخطوة 5: تحديث المحتوى

1. من لوحة التحكم → **إعدادات الموقع**:
   - أضف رقم الهاتف الحقيقي
   - أضف الإيميل الحقيقي
   - أضف رابط الفيسبوك
   - أضف واتساب (بدون +، مثال: 213555123456)

2. أضف صور المشاريع الحقيقية من **إدارة المشاريع**

3. أضف شعار الشركة من **الإعدادات**

---

## 🔄 تحديث sitemap.xml

بعد نشر الموقع، عدل `sitemap.xml`:
- استبدل `https://dftechnology.dz` بنطاقك الفعلي
- عدل `lastmod` بتاريخ اليوم

---

## ⚡ البيانات الافتراضية

الموقع يحتوي مسبقاً على:
- ✅ 4 خدمات
- ✅ 3 مشاريع نموذجية
- ✅ 5 أسئلة شائعة
- ✅ 4 شركاء نموذجيون
- ✅ كل الإعدادات والنصوص

---

## 📧 إشعارات البريد الإلكتروني

لتلقي إشعار عند وصول رسالة/طلب سعر جديد:
1. من Supabase → **Database** → **Webhooks**
2. أنشئ Webhook يرسل إشعاراً عند INSERT في جدول messages

أو استخدم **Zapier** مجاناً:
- Trigger: Supabase → New Row in messages
- Action: Gmail → Send Email

---

## 🛠️ استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| البيانات لا تُحفظ | تحقق من Supabase URL و Key |
| الصور لا ترتفع | تأكد من إنشاء bucket "media" في Supabase Storage |
| لوحة التحكم لا تفتح | تأكد أن الجلسة لم تنته (8 ساعات) |
| الموقع يعمل offline | طبيعي — يستخدم localStorage كنسخة احتياطية |

---

**دعم:** contact@dftechnology.dz
